# melos_managed_dependency_overrides: constants,localization,models,rules,utils,logger
dependency_overrides:
  constants:
    path: ../constants
  localization:
    path: ../localization
  models:
    path: ../models
  rules:
    path: ../rules
  utils:
    path: ../utils
  logger: ^2.5.0
