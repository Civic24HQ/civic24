# melos_managed_dependency_overrides: constants,localization,models,rules,utils
dependency_overrides:
  constants:
    path: ../constants
  localization:
    path: ../localization
  models:
    path: ../models
  rules:
    path: ../rules
  utils:
    path: ../utils
