name: constants
description: "Constants for Civic24"
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # General Dependencies

  # Package Dependencies
  localization:
    path: ../localization

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  # Internal Packages
  rules:
    path: ../rules

flutter:
  uses-material-design: true
