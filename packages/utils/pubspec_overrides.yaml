# melos_managed_dependency_overrides: constants,localization,models,rules
dependency_overrides:
  constants:
    path: ../constants
  localization:
    path: ../localization
  models:
    path: ../models
  rules:
    path: ../rules
