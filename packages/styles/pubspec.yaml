name: styles
description: "Syles Resources for Civic24"
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: "3.32.6"

dependencies:
  flutter:
    sdk: flutter

  # General Dependencies
  gap: ^3.0.1
  flex_color_scheme: ^8.2.0

  # Package Dependencies
  assets:
    path: ../assets
  utils:
    path: ../utils

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  # Internal Packages
  rules:
    path: ../rules

flutter:
  uses-material-design: true
